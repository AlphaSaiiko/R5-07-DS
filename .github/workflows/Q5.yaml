name: Q5 - Archive Q5.java Datée (JJ-MM-AAAA)

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  archive-q5-dated:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout du code
        uses: actions/checkout@v4

      - name: 📅 Définir le nom de fichier daté
        id: set_filename
        run: |
          # Format de la date : JJ-MM-AAAA
          DATE_FORMATTEE=$(date +'%d-%m-%Y')
          ARCHIVE_FILE="Q5_archive_${DATE_FORMATTEE}.txt"
          echo "ARCHIVE_FILE=$ARCHIVE_FILE" >> $GITHUB_OUTPUT

      - name: 📝 Créer le fichier d'archive
        run: |
          echo "Archive créée par : $NOM_PRENOM" > ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          echo "-----------------------------------" >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          echo "Contenu de Q5.java :" >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          cat Q5.java >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}

      - name: ⬆️ Pousser le fichier archivé
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'Q5: Ajout de Q5_archive_date.txt (workflow Q5)'
          file_pattern: |
            Q5_archive_*.txt
            Q5.java # Assure que le fichier source Q5.java est à jour
