name: Q4 - Archive Q4.java Datée (JJ-MM-AAAA)

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  archive-q4-dated:
    runs-on: ubuntu-latest
    env:
      NOM_PRENOM: "AlphaSaiiko"
    steps:
      - name: ⬇️ Checkout du code
        uses: actions/checkout@v4

      - name: 📅 Définir le nom de fichier daté
        id: set_filename
        run: |
          # Format de la date : JJ-MM-AAAA
          DATE_FORMATTEE=$(date +'%d-%m-%Y')
          ARCHIVE_FILE="Q4_archive_${DATE_FORMATTEE}.txt"
          echo "ARCHIVE_FILE=$ARCHIVE_FILE" >> $GITHUB_OUTPUT

      - name: 📝 Créer le fichier d'archive
        run: |
          echo "Archive créée par : $NOM_PRENOM" > ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          echo "-----------------------------------" >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          echo "Contenu de Q4.java :" >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}
          cat Q4.java >> ${{ steps.set_filename.outputs.ARCHIVE_FILE }}

      - name: ⬆️ Pousser le fichier archivé
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git config pull.rebase false
          git add Q4_archive_*.txt
          git commit -m "Q4: Ajout de Q4_archive_date.txt (workflow Q4)" || exit 0
          
          # Boucle de retry pour gérer les conflits de concurrence
          for i in {1..5}; do
            git pull --no-edit origin main && git push origin main && break
            echo "Tentative $i échouée, retry dans 5 secondes..."
            sleep 5
          done
